# Modern Course Platform

## Setup

1. **Install Node.js** — download from https://nodejs.org (LTS version)
2. Open a terminal in this folder
3. Run:

```bash
npm install
npm run dev
```

4. Open http://localhost:3000

## Customise the course

Edit `data/course.ts` — swap in your own titles, descriptions, and YouTube URLs.
